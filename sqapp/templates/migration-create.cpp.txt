/* This template file is used by sqtool-cxx
// migrations.cpp has included below headers.
#include <SqStorage.h>
#include <SqMigration.h>
#include <CStructs.h>
 */

const SqMigration {{ migration_name }}_{{ timestamp }} = {

	// Run the migrations.
	.up = [](SqSchema *schema, SqStorage *storage) {
		SqTable  *table;
/*
		table = schema->create<{{ struct_name }}>("{{ table_name }}");
		table->integer("id", &{{ struct_name }}::id)->primary()->increment();
 */
	},

	// Reverse the migrations.
	.down = [](SqSchema *schema, SqStorage *storage) {
		schema->drop("{{ table_name }}");
	},

#ifdef SQ_APP_TOOL
	.name = "{{ timestamp }}_{{ migration_name }}",
#endif
};
